#include <stdio.h>

int main() {
    int A[30], n, eleMijloc;
    printf("Introduceti numarul de pozitii: ");
    scanf("%d", &n);

    for(int i = 0, i < n; i++) { 
        scanf("%d", &A[i]);
        if (n / 2 == 0) {
            
        }
    }
}